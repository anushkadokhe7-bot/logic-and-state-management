# logic-and-state-management
its 3rd task

JavaScript Logic & State Management
Develop an interactive, client-side To-Do List application to master DOM manipulation, event handling, and local data persistence.

Key Features:
Implement full CRUD (Create, Read, Update, Delete) functionality
Persist data automatically using window.localStorage
Build advanced filtering (All, Active, Completed)
Create dynamic DOM elements and attach delegated event listeners
Expected Outcome:
A fully functional, state-driven task management application that retains user data across browser reloads.
